//
// EmailFeaturePlatformStatistics.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

@objc public class EmailFeaturePlatformStatistics: NSObject, Codable, JSONEncodable {

    public enum Platform: String, Codable, CaseIterable {
        case android = "android"
        case desktopApp = "desktop-app"
        case desktopWebmail = "desktop-webmail"
        case ios = "ios"
        case macos = "macos"
        case mobileWebmail = "mobile-webmail"
        case outlookCom = "outlook-com"
        case webmail = "webmail"
        case windows = "windows"
        case windowsMail = "windows-mail"
    }
    public var platform: Platform
    public var versions: [EmailFeatureVersionStatistics]

    public init(platform: Platform, versions: [EmailFeatureVersionStatistics]) {
        self.platform = platform
        self.versions = versions
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case platform
        case versions
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(platform, forKey: .platform)
        try container.encode(versions, forKey: .versions)
    }
}

