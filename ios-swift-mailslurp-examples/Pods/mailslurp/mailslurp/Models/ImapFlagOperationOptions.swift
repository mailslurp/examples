//
// ImapFlagOperationOptions.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** IMAP operation flags */
@objc public class ImapFlagOperationOptions: NSObject, Codable, JSONEncodable {

    public enum FlagOperation: String, Codable, CaseIterable {
        case setFlags = "SET_FLAGS"
        case addFlags = "ADD_FLAGS"
        case removeFlags = "REMOVE_FLAGS"
    }
    public var flagOperation: FlagOperation
    public var flags: [String]

    public init(flagOperation: FlagOperation, flags: [String]) {
        self.flagOperation = flagOperation
        self.flags = flags
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case flagOperation
        case flags
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(flagOperation, forKey: .flagOperation)
        try container.encode(flags, forKey: .flags)
    }
}

