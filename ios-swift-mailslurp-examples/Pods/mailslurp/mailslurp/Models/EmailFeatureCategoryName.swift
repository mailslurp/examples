//
// EmailFeatureCategoryName.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

@objc public class EmailFeatureCategoryName: NSObject, Codable, JSONEncodable {

    public enum Slug: String, Codable, CaseIterable {
        case css = "css"
        case html = "html"
        case image = "image"
        case others = "others"
    }
    public var slug: Slug
    public var name: String

    public init(slug: Slug, name: String) {
        self.slug = slug
        self.name = name
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case slug
        case name
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(slug, forKey: .slug)
        try container.encode(name, forKey: .name)
    }
}

