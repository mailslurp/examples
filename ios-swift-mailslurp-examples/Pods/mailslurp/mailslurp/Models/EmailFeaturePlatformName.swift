//
// EmailFeaturePlatformName.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

@objc public class EmailFeaturePlatformName: NSObject, Codable, JSONEncodable {

    public enum Slug: String, Codable, CaseIterable {
        case android = "android"
        case desktopApp = "desktop-app"
        case desktopWebmail = "desktop-webmail"
        case ios = "ios"
        case macos = "macos"
        case mobileWebmail = "mobile-webmail"
        case outlookCom = "outlook-com"
        case webmail = "webmail"
        case windows = "windows"
        case windowsMail = "windows-mail"
    }
    public var slug: Slug
    public var name: String

    public init(slug: Slug, name: String) {
        self.slug = slug
        self.name = name
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case slug
        case name
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(slug, forKey: .slug)
        try container.encode(name, forKey: .name)
    }
}

