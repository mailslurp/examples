//
// FlushExpiredInboxesResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Result from calling expire on any inboxes that have applicable expiration dates given current time. */
@objc public class FlushExpiredInboxesResult: NSObject, Codable, JSONEncodable {

    /** Inbox IDs affected by expiration */
    public var inboxIds: [UUID]
    /** DateTime to filter inboxes so that those expiring before this time are expired */
    public var expireBefore: Date

    public init(inboxIds: [UUID], expireBefore: Date) {
        self.inboxIds = inboxIds
        self.expireBefore = expireBefore
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case inboxIds
        case expireBefore
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(inboxIds, forKey: .inboxIds)
        try container.encode(expireBefore, forKey: .expireBefore)
    }
}

