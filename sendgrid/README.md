# MailSlurp SendGrid Test Example

See code for instructions.


Set `API_KEY`, `SENDGRID_API_KEY`, and `SENDGRID_FROM_EMAIL` (a verified SendGrid
sender) before running `npx jest --runInBand`. The live test is skipped when
SendGrid credentials or the verified sender are absent.
